functionunction setup() {
  createCanvas(800, 500);
  background("black");
}

function draw() {
  fill("pink");
  stroke("rgb(255,255,255) ");
  if(mouseIsPressed);
  circle(mouseX, mouseY, 30, 50 );
}
